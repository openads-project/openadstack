# Auto-generated by `../../scripts/render_compose_sources.py`
# based on `.docker-compose.oci-overrides.yml` with resolved includes
#   include:
#     # https://github.com/openads-project/lanelet2_object_list_prediction/blob/v1.0.0/docker/compose/docker-compose.yml
#     - oci://ghcr.io/openads-project/lanelet2_object_list_prediction:compose-open-source # TODO: compose-v1-0-0

services:

  lanelet2-object-list-prediction:
    image: ghcr.io/openads-project/lanelet2_object_list_prediction:v1.0.0
    environment:
      # --- name ------
      # NAMESPACE: /
      NAMESPACE: /understanding
      NAME: lanelet2_object_list_prediction
      # --- inputs ----
      # OBJECT_LIST_TOPIC: ~/object_list
      OBJECT_LIST_TOPIC: /understanding/simple_object_tracking/object_list
      # --- outputs ---
      PREDICTED_OBJECT_LIST_TOPIC: ~/predicted_object_list
      # --- other -----
      LOG_LEVEL: ${LOG_LEVEL:-info}
      USE_SIM_TIME: ${USE_SIM_TIME:-false}
      # PARAMS: ${PARAMS:-/docker-ros/ws/install/lanelet2_object_list_prediction/share/lanelet2_object_list_prediction/config/params.yml}
      PARAMS: /params.yml
    command:
      - /bin/bash
      - -ic
      - |
        ros2 launch lanelet2_object_list_prediction lanelet2_object_list_prediction_launch.py \
          namespace:=$${NAMESPACE} \
          name:=$${NAME} \
          log_level:=$${LOG_LEVEL} \
          use_sim_time:=$${USE_SIM_TIME} \
          params:=$${PARAMS} \
          object_list_topic:=$${OBJECT_LIST_TOPIC} \
          predicted_object_list_topic:=$${PREDICTED_OBJECT_LIST_TOPIC}
    # volumes:
    #   - ./params.yml:/docker-ros/ws/install/lanelet2_object_list_prediction/share/lanelet2_object_list_prediction/config/params.yml
    extends:
      file: ../../docker-compose-essentials/docker-compose.template.yml
      service: ros2-service
