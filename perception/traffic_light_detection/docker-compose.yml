# === Environment Variables ====================================================
# - PROFILES          (optional)
# - LAUNCH_FILE       (optional)
# - PARAMS_YML        (optional)
# - CAMERA_PARAMS_YML (required)
# - MODEL_PATH        (optional)
# ==============================================================================

services:

  perception.traffic-light-detection:
    profiles:
      - ${PROFILES:-default}
    extends:
      file: ../../docker-compose-essentials/docker-compose.template.yml
      service: ros2-gpu-service
    image: gitlab.ika.rwth-aachen.de:5050/fb-fi/its-modules/perception/traffic_light_detection:latest
    command: ros2 launch traffic_light_detection start_tld_launch.py
    volumes:
      - ${LAUNCH_FILE:-/dev/null:/dev/null}${LAUNCH_FILE:+:/docker-ros/ws/install/traffic_light_detection/share/traffic_light_detection/launch/start_tld_launch.py}
      - ${PARAMS_YML:-/dev/null:/dev/null}${PARAMS_YML:+:/docker-ros/ws/install/traffic_light_detection/share/traffic_light_detection/params/tld_main.yaml}
      - ${CAMERA_PARAMS_YML:/docker-ros/ws/install/traffic_light_detection/share/traffic_light_detection/params/cameras/camera_tld_camera.yaml}
      - ${MODEL_PATH:-/dev/null:/dev/null}${MODEL_PATH:+:/models/tld.torchscript}
