# Remote compose file with overrides defined here can be rendered by
# `../../scripts/render_compose_sources.py`

include:
  # https://github.com/openads-project/monitoring/blob/v1.0.0/docker/compose/docker-compose.yml
  - oci://ghcr.io/openads-project/monitoring:compose-v1.0.0

services:

  monitoring:
    extends:
      file: ../../docker-compose-essentials/docker-compose.template.yml
      service: ros2-gpu-x11-service
    environment:
      # --- name ------
      NAMESPACE: /monitoring
      # --- other -----
      CONFIG: /conf.rviz