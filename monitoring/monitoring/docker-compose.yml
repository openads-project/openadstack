# Auto-generated by `../../scripts/render_compose_sources.py`
# based on `.docker-compose.oci-overrides.yml` with resolved includes
#   include:
#     # https://github.com/openads-project/monitoring/blob/init/docker/compose/docker-compose.yml
#     - oci://ghcr.io/openads-project/monitoring:compose-init
#     # TODO: change both lines to tagged version

services:

  monitoring:
    image: ghcr.io/openads-project/monitoring:latest_init_ci  # TODO: change to tagged version
    environment:
      # --- name ------
      # NAMESPACE: /
      NAMESPACE: /monitoring
      NAME: monitoring
      # --- other -----
      LOG_LEVEL: ${LOG_LEVEL:-info}
      USE_SIM_TIME: ${USE_SIM_TIME:-false}
      # CONFIG: ${CONFIG:-}
      CONFIG: /conf.rviz
    command:
      - /bin/bash
      - -ic
      - |
        ros2 launch monitoring monitoring.launch.py \
          namespace:=$${NAMESPACE} \
          name:=$${NAME} \
          log_level:=$${LOG_LEVEL} \
          use_sim_time:=$${USE_SIM_TIME} \
          config:=$${CONFIG}
    extends:
      file: ../../docker-compose-essentials/docker-compose.template.yml
      service: ros2-gpu-x11-service
    volumes:
      - ./config/conf.rviz:/conf.rviz
