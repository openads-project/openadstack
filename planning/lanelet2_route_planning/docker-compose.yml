include:
  # Override LANELET2_ROUTE_PLANNING_COMPOSE with a local file while developing the upstream contract.
  - ${LANELET2_ROUTE_PLANNING_COMPOSE:-oci://ghcr.io/openads-project/lanelet2_route_planning-compose:v1.0.2}

services:

  planning.lanelet2-route-planning:
    extends:
      file: ../../docker-compose-essentials/docker-compose.template.yml
      service: ros2-service
    environment:
      # --- inputs ----
      EGO_DATA_TOPIC: /localization/ego_state_estimation/ego_data
