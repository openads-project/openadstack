# Auto-generated by `../../utils/scripts/render_compose_sources.py`
# based on `.docker-compose.oci-overrides.yml` with resolved includes
#   include:
#     # https://github.com/openads-project/lanelet2_route_planning/blob/v2.0.0/deployment/compose/docker-compose.yml
#     - oci://ghcr.io/openads-project/lanelet2_route_planning/compose:v2.0.0

services:

  planning.lanelet2-route-planning:
    extends:
      file: ../../utils/compose/docker-compose.template.yml
      service: ros2-service
    image: ghcr.io/openads-project/lanelet2_route_planning:improve-action-client
    environment:
      # --- name ------
      # NAMESPACE: /
      NAMESPACE: /planning
      NAME: lanelet2_route_planning
      # --- inputs ----
      # EGO_DATA_TOPIC: ~/ego_data
      EGO_DATA_TOPIC: /localization/ego_state_estimation/ego_data
      # --- outputs ---
      ROUTE_TOPIC: ~/route
      GLOBAL_ROUTE_TOPIC: ~/global_route
      DIAGNOSTICS_TOPIC: /diagnostics
      # --- other -----
      LOG_LEVEL: ${LOG_LEVEL:-info}
      USE_SIM_TIME: ${USE_SIM_TIME:-false}
      PARAMS: ${PARAMS:-/docker-ros/ws/install/lanelet2_route_planning/share/lanelet2_route_planning/config/params.yml}
    command:
      - /bin/bash
      - -ic
      - |
        ros2 launch lanelet2_route_planning lanelet2_route_planning_launch.py \
          namespace:=$${NAMESPACE} \
          name:=$${NAME} \
          log_level:=$${LOG_LEVEL} \
          use_sim_time:=$${USE_SIM_TIME} \
          params:=$${PARAMS} \
          ego_data_topic:=$${EGO_DATA_TOPIC} \
          route_topic:=$${ROUTE_TOPIC} \
          global_route_topic:=$${GLOBAL_ROUTE_TOPIC} \
          diagnostics_topic:=$${DIAGNOSTICS_TOPIC}
    # volumes:
    #   - ./params.yml:/docker-ros/ws/install/lanelet2_route_planning/share/lanelet2_route_planning/config/params.yml
