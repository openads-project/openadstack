# Auto-generated by `../../utils/scripts/render_compose_sources.py`
# based on `.docker-compose.oci-overrides.yml` with resolved includes
#   include:
#     # https://github.com/openads-project/simple_planner/blob/v1.4.0/deployment/compose/docker-compose.yml
#     - oci://ghcr.io/openads-project/simple_planner/compose:v1.4.0

services:

  planning.simple-planner:
    extends:
      file: ../../utils/compose/docker-compose.template.yml
      service: ros2-service
    image: ghcr.io/openads-project/simple_planner:latest_tracing_ci
    environment:
      # --- name ------
      # NAMESPACE: /
      NAMESPACE: /planning
      NAME: simple_planner
      # --- inputs ----
      # EGO_DATA_TOPIC: ~/ego_data
      EGO_DATA_TOPIC: /localization/ego_state_estimation/ego_data
      # OBJECT_LIST_TOPIC: ~/object_list
      OBJECT_LIST_TOPIC: /understanding/lanelet2_object_list_prediction/object_list
      # ROUTE_TOPIC: ~/route
      ROUTE_TOPIC: /planning/lanelet2_route_planning/route
      GRID_MAP_TOPIC: ~/grid_map
      # --- outputs ---
      TRAJECTORY_TOPIC: ~/trajectory
      OBJECT_INTERACTION_MARKERS_TOPIC: ~/viz/object_interaction_markers
      # --- other -----
      LEFT_TURN_INDICATOR_SERVICE: ~/enable_left_turn_indicator
      RIGHT_TURN_INDICATOR_SERVICE: ~/enable_right_turn_indicator
      HAZARD_LIGHTS_SERVICE: ~/enable_hazard_lights
      LOG_LEVEL: ${LOG_LEVEL:-info}
      USE_SIM_TIME: ${USE_SIM_TIME:-false}
      PARAMS: ${PARAMS:-/docker-ros/ws/install/simple_planner/share/simple_planner/config/params.yml}
      ROS_TRACING: ${ROS_TRACING:-false}
    command:
      - /bin/bash
      - -ic
      - |
        ros2 launch simple_planner simple_planner.launch.py \
          namespace:=$${NAMESPACE} \
          name:=$${NAME} \
          log_level:=$${LOG_LEVEL} \
          use_sim_time:=$${USE_SIM_TIME} \
          params:=$${PARAMS} \
          ros_tracing:=$${ROS_TRACING} \
          ego_data_topic:=$${EGO_DATA_TOPIC} \
          object_list_topic:=$${OBJECT_LIST_TOPIC} \
          route_topic:=$${ROUTE_TOPIC} \
          grid_map_topic:=$${GRID_MAP_TOPIC} \
          trajectory_topic:=$${TRAJECTORY_TOPIC} \
          object_interaction_markers_topic:=$${OBJECT_INTERACTION_MARKERS_TOPIC} \
          left_turn_indicator_service:=$${LEFT_TURN_INDICATOR_SERVICE} \
          right_turn_indicator_service:=$${RIGHT_TURN_INDICATOR_SERVICE} \
          hazard_lights_service:=$${HAZARD_LIGHTS_SERVICE}
    # volumes:
    #   - ./params.yml:/docker-ros/ws/install/simple_planner/share/simple_planner/config/params.yml
