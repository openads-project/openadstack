# Remote compose file with overrides defined here can be rendered by
# `../../scripts/render_compose_sources.py`

include:
  # https://github.com/openads-project/lanelet2_map_server/blob/v1.2.0/docker/compose/docker-compose.yml
  - oci://ghcr.io/openads-project/lanelet2_map_server:compose-main # TODO: compose-v1.2.0

services:

  lanelet2-map-server:
    extends:
      file: ../../docker-compose-essentials/docker-compose.template.yml
      service: ros2-service
    environment:
      # --- name ------
      NAMESPACE: /localization
      # --- inputs ----
      NAV_SAT_FIX_TOPIC: /localization/ego_state_estimation/nav_sat_fix
